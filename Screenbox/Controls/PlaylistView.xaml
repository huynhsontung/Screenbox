<UserControl
    x:Class="Screenbox.Controls.PlaylistView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Screenbox.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Screenbox.ViewModels"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <ListView
            Width="200"
            AllowDrop="True"
            CanDragItems="True"
            CanReorderItems="True"
            ItemsSource="{x:Bind ViewModel.Playlist}"
            SelectedItem="{x:Bind ViewModel.CurrentlyPlaying, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:MediaViewModel">
                    <Grid>
                        <SymbolIcon
                            HorizontalAlignment="Left"
                            Symbol="Play"
                            Visibility="{x:Bind IsPlaying, Converter={StaticResource CommonVisibilityConverter}, Mode=OneWay}" />
                        <TextBlock Margin="30,0,0,0" Text="{x:Bind Title}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
