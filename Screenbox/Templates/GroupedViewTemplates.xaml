<ResourceDictionary
    x:Class="Screenbox.Templates.GroupedViewTemplates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Screenbox.Behaviors"
    xmlns:collections="using:System.Collections"
    xmlns:common="using:Screenbox.Core.Common"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Screenbox.Templates">

    <!--  ListViewBase Header  -->
    <DataTemplate x:Key="GroupHeaderTemplate" x:DataType="common:IStringKey">
        <TextBlock
            FontWeight="SemiBold"
            Text="{x:Bind Key}"
            TextTrimming="CharacterEllipsis">
            <interactivity:Interaction.Behaviors>
                <behaviors:OverflowTextToolTipBehavior />
            </interactivity:Interaction.Behaviors>
        </TextBlock>
    </DataTemplate>

    <!--  SemanticZoom ZoomedOutView  -->
    <DataTemplate x:Key="GroupOverviewLetterItemTemplate" x:DataType="ICollectionViewGroup">
        <Button
            MinWidth="{StaticResource GridViewHeaderItemMinHeight}"
            MinHeight="{StaticResource GridViewHeaderItemMinHeight}"
            HorizontalAlignment="Stretch"
            ClickMode="Hover"
            FontSize="{StaticResource SubtitleTextBlockFontSize}"
            FontWeight="SemiBold"
            IsEnabled="{x:Bind local:GroupedViewTemplates.IsIntegerNormal(Group.(collections:ICollection.Count))}"
            Style="{StaticResource SubtleButtonStyle}">
            <TextBlock Text="{x:Bind ((common:IStringKey)Group).Key}" TextTrimming="CharacterEllipsis">
                <interactivity:Interaction.Behaviors>
                    <behaviors:OverflowTextToolTipBehavior />
                </interactivity:Interaction.Behaviors>
            </TextBlock>
        </Button>
    </DataTemplate>
</ResourceDictionary>
