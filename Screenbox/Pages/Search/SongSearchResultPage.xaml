<Page
    x:Class="Screenbox.Pages.SongSearchResultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Screenbox.Behaviors"
    xmlns:commands="using:Screenbox.Commands"
    xmlns:contract13NotPresent="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractNotPresent(Windows.Foundation.UniversalApiContract,13)"
    xmlns:controls="using:Screenbox.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:flyouts="using:Screenbox.Flyouts"
    xmlns:helpers="using:Screenbox.Helpers"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:strings="using:Screenbox.Strings"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Page.Resources>
        <commands:BindableCommand x:Key="PlaySongCommand" Command="{x:Bind ViewModel.PlaySongCommand}" />

        <flyouts:MediaMenuFlyout
            x:Key="ItemFlyout"
            AddToQueueCommand="{x:Bind Common.AddToQueueCommand}"
            ContextItem="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
            IsAdvancedModeEnabled="{x:Bind Common.IsAdvancedModeEnabled}"
            PlayCommand="{x:Bind ViewModel.PlayCommand}"
            PlayNextCommand="{x:Bind Common.PlayNextCommand}">
            <MenuFlyoutItem
                Command="{x:Bind Common.OpenAlbumCommand}"
                CommandParameter="{x:Bind ViewModel.ContextMedia.Album, Mode=OneWay}"
                Icon="{ui:FontIcon Glyph=&#xE93C;}"
                Text="{x:Bind strings:Resources.ShowAlbum}"
                Visibility="{x:Bind ViewModel.ContextMedia.Album, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
            <MenuFlyoutItem
                Command="{x:Bind Common.OpenArtistCommand}"
                CommandParameter="{x:Bind ViewModel.ContextMedia.MainArtist, Mode=OneWay}"
                Icon="{ui:FontIcon Glyph=&#xE77B;}"
                Text="{x:Bind strings:Resources.ShowArtist}"
                Visibility="{x:Bind ViewModel.ContextMedia.MainArtist, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
        </flyouts:MediaMenuFlyout>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="HeaderText"
            Grid.Row="0"
            MinHeight="{StaticResource PageHeaderMinHeight}"
            Margin="{StaticResource BottomMediumMargin}"
            Padding="{StaticResource ContentPagePadding}"
            MaxLines="2"
            Style="{StaticResource TitleMediumTextBlockStyle}"
            Text="{x:Bind strings:Resources.SearchResultSongHeader(ViewModel.SearchQuery), FallbackValue={}}">
            <interactivity:Interaction.Behaviors>
                <behaviors:OverflowTextToolTipBehavior />
            </interactivity:Interaction.Behaviors>
        </TextBlock>

        <ListView
            x:Name="SongListView"
            Grid.Row="1"
            Margin="{StaticResource TopLargeMargin}"
            Padding="{StaticResource ContentPagePadding}"
            ui:ListViewExtensions.ItemContainerStretchDirection="Horizontal"
            ui:ScrollViewerExtensions.VerticalScrollBarMargin="{x:Bind Common.ScrollBarMargin, Mode=OneWay}"
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind ViewModel.SearchResult.Songs, FallbackValue={x:Null}}"
            SelectionMode="None"
            Style="{StaticResource MediaListViewStyle}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:MediaListViewItem PlayCommand="{StaticResource PlaySongCommand}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Footer>
                <Grid Height="{x:Bind Common.FooterBottomPaddingHeight, Mode=OneWay}" />
            </ListView.Footer>
            <interactivity:Interaction.Behaviors>
                <behaviors:AlternatingListViewBehavior
                    AlternateBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    AlternateBorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    AlternateBorderThickness="1" />
                <behaviors:ListViewContextBehavior ContextItem="{x:Bind ViewModel.ContextMedia, Mode=TwoWay}" Flyout="{StaticResource ItemFlyout}" />
            </interactivity:Interaction.Behaviors>
        </ListView>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="NavigationViewStates">
                <VisualState x:Name="Expanded" />
                <VisualState x:Name="Compact" />
                <VisualState x:Name="Minimal">
                    <VisualState.StateTriggers>
                        <ui:IsEqualStateTrigger Value="{x:Bind Common.NavigationViewDisplayMode, Mode=OneWay}" To="{StaticResource MinimalNavigationViewDisplayMode}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderText.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="SongListView.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>

