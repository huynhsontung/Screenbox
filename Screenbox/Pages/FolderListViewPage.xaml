<Page
    x:Class="Screenbox.Pages.FolderListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Screenbox.Behaviors"
    xmlns:commands="using:Screenbox.Commands"
    xmlns:contract13NotPresent="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractNotPresent(Windows.Foundation.UniversalApiContract,13)"
    xmlns:converters="using:Screenbox.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="using:Screenbox.Extensions"
    xmlns:flyouts="using:Screenbox.Flyouts"
    xmlns:helpers="using:Screenbox.Helpers"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:strings="using:Screenbox.Strings"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewModels="using:Screenbox.Core.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <flyouts:MediaMenuFlyout
            x:Key="ItemFlyout"
            AddToQueueCommand="{x:Bind ViewModel.AddToQueueCommand}"
            ContextItem="{x:Bind ViewModel.ContextItem, Mode=OneWay}"
            IsAdvancedModeEnabled="{x:Bind Common.IsAdvancedModeEnabled}"
            PlayCommand="{x:Bind ViewModel.PlayCommand}"
            PlayNextCommand="{x:Bind ViewModel.PlayNextCommand}" />

        <DataTemplate x:Key="StorageItemListViewItemTemplate" x:DataType="viewModels:StorageItemViewModel">
            <Grid AutomationProperties.Name="{x:Bind helpers:ItemLabelHelper.FormatAccessibleStorageItemName(IsFile, Name, CaptionText, ItemCount)}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" MinWidth="54" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    Grid.Column="0"
                    HorizontalAlignment="Center"
                    Glyph="{x:Bind converters:GlyphConverter.ToStorageItemGlyph(StorageItem)}" />

                <TextBlock
                    Grid.Column="1"
                    Margin="8,0,16,0"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{x:Bind Name}"
                    TextWrapping="NoWrap">
                    <interactivity:Interaction.Behaviors>
                        <behaviors:OverflowTextToolTipBehavior />
                    </interactivity:Interaction.Behaviors>
                </TextBlock>

                <TextBlock
                    Grid.Column="2"
                    HorizontalAlignment="Right"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{x:Bind CaptionText, Mode=OneWay}"
                    TextWrapping="NoWrap" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <muxc:ProgressBar
            VerticalAlignment="Top"
            IsIndeterminate="True"
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
        <ListView
            x:Name="ListView"
            Padding="{StaticResource ContentPagePadding}"
            ui:ListViewExtensions.Command="{x:Bind ViewModel.ClickCommand}"
            ui:ListViewExtensions.ItemContainerStretchDirection="Horizontal"
            ui:ScrollViewerExtensions.VerticalScrollBarMargin="{x:Bind Common.ScrollBarMargin, Mode=OneWay}"
            IsItemClickEnabled="True"
            ItemTemplate="{StaticResource StorageItemListViewItemTemplate}"
            ItemsSource="{x:Bind ViewModel.Items}"
            Loaded="ListView_OnLoaded"
            SelectionMode="None"
            Style="{StaticResource MediaListViewStyle}">
            <ListView.Footer>
                <Border Height="{x:Bind Common.FooterBottomPaddingHeight, Mode=OneWay}" />
            </ListView.Footer>
            <interactivity:Interaction.Behaviors>
                <behaviors:AutoFocusBehavior />
                <behaviors:AlternatingListViewBehavior
                    AlternateBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    AlternateBorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    AlternateBorderThickness="1" />
                <behaviors:ListViewContextBehavior
                    ContextItem="{x:Bind ViewModel.ContextItem, Mode=TwoWay}"
                    ContextRequested="FolderView_OnItemContextRequested"
                    Flyout="{StaticResource ItemFlyout}" />
                <behaviors:BringIntoViewWithOffsetBehavior FromBottom="{x:Bind Common.FooterBottomPaddingHeight, Mode=OneWay}" />
                <behaviors:FocusOnItemClickBehavior />
            </interactivity:Interaction.Behaviors>
        </ListView>

        <!--  Empty content  -->
        <TextBlock
            x:Name="NoContentText"
            Margin="{StaticResource TopLargeMargin}"
            Padding="{StaticResource ContentPagePadding}"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Text="{x:Bind strings:Resources.EmptyFolder}"
            Visibility="{x:Bind ViewModel.IsEmpty, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="NavigationViewStates">
                <VisualState x:Name="Expanded" />
                <VisualState x:Name="Compact" />
                <VisualState x:Name="Minimal">
                    <VisualState.StateTriggers>
                        <ui:IsEqualStateTrigger Value="{x:Bind Common.NavigationViewDisplayMode, Mode=OneWay}" To="{StaticResource MinimalNavigationViewDisplayMode}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ListView.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="NoContentText.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>

