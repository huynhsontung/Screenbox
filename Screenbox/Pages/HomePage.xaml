<Page
    x:Class="Screenbox.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Screenbox.Behaviors"
    xmlns:commands="using:Screenbox.Commands"
    xmlns:contract13NotPresent="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractNotPresent(Windows.Foundation.UniversalApiContract,13)"
    xmlns:controls="using:Screenbox.Controls"
    xmlns:converters="using:Screenbox.Converters"
    xmlns:ctAnimations="using:CommunityToolkit.WinUI.Animations"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="using:Screenbox.Extensions"
    xmlns:helpers="using:Screenbox.Helpers"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Screenbox.Core.Models"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:primitives="using:Windows.UI.Xaml.Controls.Primitives"
    xmlns:strings="using:Screenbox.Strings"
    xmlns:templates="using:Screenbox.Templates"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewModels="using:Screenbox.Core.ViewModels"
    Loaded="{x:Bind ViewModel.OnLoaded}"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <templates:GridViewItemTemplates />
                <ResourceDictionary Source="ms-appx:///Styles/CommandBar.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <MenuFlyout x:Key="ItemFlyout">
                <interactivity:Interaction.Behaviors>
                    <behaviors:AddToPlaylistFlyoutBehavior TargetSubItem="{x:Bind AddToPlaylistSubMenu}" />
                </interactivity:Interaction.Behaviors>

                <MenuFlyoutItem
                    Command="{x:Bind ViewModel.PlayCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Text="{x:Bind helpers:ItemLabelHelper.GetPlayPauseLabel(ViewModel.ContextMedia.IsPlaying), Mode=OneWay}">
                    <MenuFlyoutItem.Icon>
                        <FontIcon FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}" Glyph="{x:Bind converters:GlyphConverter.ToPlayPauseGlyph(ViewModel.ContextMedia.IsPlaying), Mode=OneWay}" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem
                    Command="{x:Bind Common.PlayNextCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource PlayAddGlyph}}"
                    Text="{strings:Resources Key=PlayNext}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.AddToQueueCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Text="{x:Bind strings:Resources.AddToQueue}">
                    <MenuFlyoutItem.Icon>
                        <FontIcon
                            x:Name="AddToQueueMenuFlyoutItemIcon"
                            FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                            Glyph="{StaticResource PlaylistMusicAddGlyph}" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutSubItem x:Name="AddToPlaylistSubMenu" Text="{x:Bind strings:Resources.AddToPlaylist}">
                    <MenuFlyoutSubItem.Icon>
                        <FontIcon
                            x:Name="AddToPlaylistMenuFlyoutSubItemIcon"
                            FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                            Glyph="{StaticResource PlaylistAddGlyph}" />
                    </MenuFlyoutSubItem.Icon>
                </MenuFlyoutSubItem>
                <MenuFlyoutItem
                    Command="{StaticResource OpenWithCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Icon="{ui:FontIcon Glyph=&#xE7AC;,
                                       MirroredWhenRightToLeft=True}"
                    Text="{x:Bind strings:Resources.OpenWith}" />
                <MenuFlyoutItem
                    Command="{x:Bind ViewModel.RemoveCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Icon="{ui:SymbolIcon Symbol=Clear}"
                    Text="{strings:Resources Key=Remove}" />
                <MenuFlyoutItem
                    Command="{StaticResource ShowPropertiesCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Icon="{ui:FontIcon Glyph=&#xE946;}"
                    Text="{strings:Resources Key=Properties}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.OpenAlbumCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia.Album, Mode=OneWay}"
                    Icon="{ui:FontIcon Glyph=&#xE93C;}"
                    Text="{strings:Resources Key=ShowAlbum}"
                    Visibility="{x:Bind ViewModel.ContextMedia.Album, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.OpenArtistCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia.MainArtist, Mode=OneWay}"
                    Icon="{ui:FontIcon Glyph=&#xE77B;}"
                    Text="{strings:Resources Key=ShowArtist}"
                    Visibility="{x:Bind ViewModel.ContextMedia.MainArtist, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                <MenuFlyoutSeparator Visibility="{x:Bind Common.IsAdvancedModeEnabled}" />
                <MenuFlyoutItem
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Icon="{ui:SymbolIcon Symbol=Setting}"
                    Text="{strings:Resources Key=SetPlaybackOptions}"
                    Visibility="{x:Bind Common.IsAdvancedModeEnabled}">
                    <MenuFlyoutItem.Command>
                        <commands:SetPlaybackOptionsCommand PlayCommand="{x:Bind ViewModel.PlayCommand}" />
                    </MenuFlyoutItem.Command>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    Command="{x:Bind ViewModel.Selection.SelectItemCommand}"
                    CommandParameter="{x:Bind ViewModel.ContextMedia, Mode=OneWay}"
                    Text="{x:Bind strings:Resources.Select}">
                    <MenuFlyoutItem.Icon>
                        <FontIcon
                            x:Name="SelectMenuFlyoutItemIcon"
                            FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                            Glyph="{StaticResource SingleSelectGlyph}" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuFlyout>

            <MenuFlyout x:Key="OpenFlyout" Placement="{x:Bind helpers:GlobalizationHelper.MirrorWhenRightToLeft(primitives:FlyoutPlacementMode.BottomEdgeAlignedRight)}">
                <MenuFlyoutItem
                    AccessKey="{strings:KeyboardResources Key=MenuItemFileKey}"
                    Command="{x:Bind Common.OpenFilesCommand}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource FolderOpenGlyph}}"
                    Text="{strings:Resources Key=OpenFiles}" />
                <MenuFlyoutItem
                    AccessKey="{strings:KeyboardResources Key=MenuItemFolderKey}"
                    Command="{x:Bind ViewModel.OpenFolderCommand}"
                    Text="{strings:Resources Key=OpenFolder}">
                    <MenuFlyoutItem.Icon>
                        <FontIcon
                            x:Name="OpenMenuFlyoutFolderItemIcon"
                            FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                            Glyph="{StaticResource FolderListGlyph}" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem
                    AccessKey="{strings:KeyboardResources Key=MenuItemUrlKey}"
                    Icon="{ui:SymbolIcon Symbol=Globe}"
                    KeyTipPlacementMode="Left"
                    Text="{strings:Resources Key=OpenUrl}">
                    <MenuFlyoutItem.Command>
                        <commands:OpenUrlCommand NextCommand="{x:Bind ViewModel.OpenUrlCommand}" />
                    </MenuFlyoutItem.Command>
                </MenuFlyoutItem>
            </MenuFlyout>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="HeaderGrid"
            Grid.Row="0"
            MinHeight="{StaticResource PageHeaderMinHeight}"
            Margin="{StaticResource BottomMediumMargin}"
            Padding="{StaticResource ContentPagePadding}">
            <TextBlock
                x:Name="HeaderText"
                Style="{StaticResource TitleMediumTextBlockStyle}"
                Text="{strings:Resources Key=Home}" />
            <muxc:SplitButton
                x:Name="HeaderOpenButton"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                extensions:SplitButtonExtensions.PrimaryButtonToolTip="{strings:Resources Key=OpenFilesToolTip}"
                extensions:SplitButtonExtensions.SecondaryButtonAccessKey="{strings:KeyboardResources Key=CommandMoreOptionsKey}"
                extensions:SplitButtonExtensions.SecondaryButtonToolTip="{strings:Resources Key=OpenFilesSecondaryToolTip}"
                AccessKey="{strings:KeyboardResources Key=CommandAddOpenFilesKey}"
                AutomationProperties.HelpText="{strings:Resources Key=OpenFilesToolTip}"
                AutomationProperties.Name="{x:Bind HeaderOpenButtonText.Text}"
                Command="{x:Bind Common.OpenFilesCommand}"
                Flyout="{StaticResource OpenFlyout}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon
                        Margin="{StaticResource IconButtonStandardIconMargin}"
                        FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                        Glyph="{StaticResource FolderOpenGlyph}" />
                    <TextBlock
                        x:Name="HeaderOpenButtonText"
                        Margin="8,0,0,0"
                        Text="{strings:Resources Key=OpenFiles}" />
                </StackPanel>
                <interactivity:Interaction.Behaviors>
                    <behaviors:SplitButtonXYNavigationBehavior />
                    <behaviors:AutoFocusBehavior />
                </interactivity:Interaction.Behaviors>
            </muxc:SplitButton>
        </Grid>

        <Grid
            x:Name="RecentSubHeaderGrid"
            Grid.Row="1"
            MinHeight="48"
            Padding="{StaticResource ContentPagePadding}">
            <TextBlock
                x:Name="GridViewHeaderText"
                VerticalAlignment="Center"
                AutomationProperties.HeadingLevel="Level2"
                Style="{StaticResource SubtitleTextBlockStyle}"
                Text="{strings:Resources Key=Recent}" />
            <controls:SelectionBarControl
                x:Name="SelectionBar"
                ctAnimations:Implicit.ShowAnimations="{StaticResource SelectionBarEntranceImplicitAnimation}"
                AddToQueueButtonCommand="{x:Bind ViewModel.AddSelectedToQueueCommand}"
                CheckBoxCommand="{StaticResource SelectDeselectAllCommand}"
                CheckBoxCommandParameter="{x:Bind RecentFilesGridView}"
                CheckBoxIsChecked="{x:Bind ViewModel.Selection.IsAllSelected, Mode=OneWay}"
                CheckBoxText="{x:Bind strings:Resources.ItemsSelected(ViewModel.Selection.SelectedItems.Count), Mode=OneWay}"
                CloseButtonCommand="{x:Bind ViewModel.Selection.ClearSelectionCommand}"
                CommandParameter="{x:Bind RecentFilesGridView.SelectedItems}"
                PlayButtonCommand="{x:Bind ViewModel.PlaySelectedCommand}"
                PlayNextButtonCommand="{x:Bind ViewModel.PlaySelectedNextCommand}"
                RemoveButtonCommand="{x:Bind ViewModel.RemoveSelectedCommand}"
                Visibility="Collapsed" />
        </Grid>

        <GridView
            x:Name="RecentFilesGridView"
            Grid.Row="2"
            Margin="0,16,0,0"
            Padding="{StaticResource GridViewContentPagePadding}"
            ui:ListViewExtensions.Command="{x:Bind ViewModel.PlayCommand}"
            ui:ScrollViewerExtensions.VerticalScrollBarMargin="{x:Bind Common.ScrollBarMargin, Mode=OneWay}"
            IsItemClickEnabled="True"
            ItemContainerStyle="{StaticResource GridViewItemMediaCardStyle}"
            ItemTemplate="{StaticResource MediaGridViewItemTemplate}"
            ItemsSource="{x:Bind ViewModel.Recent}"
            KeyboardAcceleratorPlacementMode="Hidden"
            SelectionMode="Extended">
            <GridView.Resources>
                <commands:BindableCommand x:Key="MediaListViewItemPlayCommand" Command="{x:Bind ViewModel.PlayCommand}" />
            </GridView.Resources>
            <GridView.Footer>
                <Border Height="{x:Bind Common.FooterBottomPaddingHeight, Mode=OneWay}" />
            </GridView.Footer>
            <GridView.KeyboardAccelerators>
                <KeyboardAccelerator
                    Key="A"
                    Invoked="SelectDeselectAllKeyboardAccelerator_OnInvoked"
                    Modifiers="Control" />
            </GridView.KeyboardAccelerators>
            <interactivity:Interaction.Behaviors>
                <behaviors:ListViewContextBehavior ContextItem="{x:Bind ViewModel.ContextMedia, Mode=TwoWay}" Flyout="{StaticResource ItemFlyout}" />
                <behaviors:ListViewSelectionBehavior SelectedItems="{x:Bind ViewModel.Selection.SelectedItems}" />
                <behaviors:BringIntoViewWithOffsetBehavior FromBottom="{x:Bind Common.FooterBottomPaddingHeight, Mode=OneWay}" />
            </interactivity:Interaction.Behaviors>
        </GridView>

        <!--  Empty recent list  -->
        <ScrollViewer
            x:Name="WelcomeViewer"
            Grid.Row="2"
            Margin="{x:Bind Common.FooterBottomPaddingMargin, Mode=OneWay}"
            Padding="{StaticResource ContentPagePadding}"
            HorizontalScrollBarVisibility="Disabled"
            HorizontalScrollMode="Disabled"
            VerticalScrollBarVisibility="Auto"
            Visibility="Collapsed">
            <controls:ContentUnavailableView
                x:Name="WelcomePanel"
                Title="{x:Bind strings:Resources.WelcomePanelHeader}"
                IsOpen="False"
                Message="{x:Bind strings:Resources.WelcomePanelSubtext}">
                <FontIcon
                    x:Name="WelcomePanelIcon"
                    Margin="0,-16,-8,0"
                    FontFamily="Segoe UI Emoji"
                    FontSize="{StaticResource NoContentPanelIconFontSize}"
                    Glyph="&#x1F44B;" />
                <controls:ContentUnavailableView.ActionContent>
                    <muxc:SplitButton
                        extensions:SplitButtonExtensions.PrimaryButtonToolTip="{x:Bind strings:Resources.OpenFilesToolTip}"
                        extensions:SplitButtonExtensions.SecondaryButtonAccessKey="{x:Bind strings:KeyboardResources.CommandMoreOptionsKey}"
                        extensions:SplitButtonExtensions.SecondaryButtonToolTip="{x:Bind strings:Resources.OpenFilesSecondaryToolTip}"
                        AccessKey="{x:Bind strings:KeyboardResources.CommandAddOpenFilesKey}"
                        AutomationProperties.HelpText="{x:Bind strings:Resources.OpenFilesToolTip}"
                        AutomationProperties.Name="{x:Bind strings:Resources.OpenFiles}"
                        Command="{x:Bind Common.OpenFilesCommand}"
                        Flyout="{StaticResource OpenFlyout}"
                        Style="{StaticResource AccentSplitButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon
                                Margin="{StaticResource IconButtonStandardIconMargin}"
                                FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                                Glyph="{StaticResource FolderOpenGlyph}" />
                            <TextBlock Margin="8,0,0,0" Text="{x:Bind strings:Resources.OpenFiles}" />
                        </StackPanel>
                        <interactivity:Interaction.Behaviors>
                            <behaviors:SplitButtonXYNavigationBehavior />
                            <behaviors:AutoFocusBehavior />
                        </interactivity:Interaction.Behaviors>
                    </muxc:SplitButton>
                </controls:ContentUnavailableView.ActionContent>
            </controls:ContentUnavailableView>
        </ScrollViewer>

        <interactivity:Interaction.Behaviors>
            <behaviors:GamepadXYNavigationBehavior />
        </interactivity:Interaction.Behaviors>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="NavigationViewStates">
                <VisualState x:Name="Expanded" />
                <VisualState x:Name="Compact" />
                <VisualState x:Name="Minimal">
                    <VisualState.StateTriggers>
                        <ui:IsEqualStateTrigger Value="{x:Bind Common.NavigationViewDisplayMode, Mode=OneWay}" To="{StaticResource MinimalNavigationViewDisplayMode}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderGrid.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="HeaderOpenButtonText.Visibility" Value="Collapsed" />
                        <Setter Target="RecentSubHeaderGrid.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="RecentFilesGridView.Padding" Value="{StaticResource GridViewContentPageMinimalPadding}" />
                        <Setter Target="WelcomeViewer.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="WelcomePanelIcon.Margin" Value="0,0,-16,0" />
                        <Setter Target="WelcomePanelIcon.FontSize" Value="{StaticResource NoContentPanelMinimalIconFontSize}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup x:Name="SelectionModes">
                <VisualState x:Name="SingleSelection" />
                <VisualState x:Name="MultipleSelection">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind ViewModel.Selection.IsSelectionModeActive, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="GridViewHeaderText.Visibility" Value="Collapsed" />
                        <Setter Target="SelectionBar.Visibility" Value="Visible" />
                        <Setter Target="RecentFilesGridView.SelectionMode" Value="Multiple" />
                        <Setter Target="RecentFilesGridView.IsItemClickEnabled" Value="False" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup x:Name="ContentStates">
                <VisualState x:Name="Welcome">
                    <VisualState.StateTriggers>
                        <ui:IsNullOrEmptyStateTrigger Value="{x:Bind ViewModel.Recent, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderOpenButton.Visibility" Value="Collapsed" />
                        <Setter Target="RecentSubHeaderGrid.Visibility" Value="Collapsed" />
                        <Setter Target="WelcomeViewer.Visibility" Value="Visible" />
                        <Setter Target="WelcomePanel.IsOpen" Value="True" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup x:Name="FlowDirectionStates">
                <VisualState x:Name="LeftToRight" />

                <VisualState x:Name="RightToLeft">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind helpers:GlobalizationHelper.IsRightToLeftLanguage}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="OpenMenuFlyoutFolderItemIcon.Glyph" Value="{StaticResource FolderListMirroredGlyph}" />
                        <Setter Target="AddToQueueMenuFlyoutItemIcon.Glyph" Value="{StaticResource PlaylistMusicAddMirroredGlyph}" />
                        <contract13NotPresent:Setter Target="AddToPlaylistMenuFlyoutSubItemIcon.Glyph" Value="{StaticResource PlaylistAddMirroredGlyph}" />
                        <Setter Target="SelectMenuFlyoutItemIcon.Glyph" Value="{StaticResource SingleSelectMirroredGlyph}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
